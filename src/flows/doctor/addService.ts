import { proto, WASocket } from "@whiskeysockets/baileys";
import { Session } from "../../types/session.interface";

export const addService = async (sock: WASocket, messageInfo: proto.IWebMessageInfo, session: Session) => {
  
}